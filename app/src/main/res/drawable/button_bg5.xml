<?xml version="1.0" encoding="utf-8"?>
<shape xmlns:android="http://schemas.android.com/apk/res/android"
    android:shape="rectangle">

    <!-- Stroke -->
    <stroke
        android:width="@dimen/dim_1"
        android:color="@color/white" />
    <!-- Rounded corners -->
    <corners
        android:radius="@dimen/dim_7"
        android:topLeftRadius="@dimen/dim_7"
        android:topRightRadius="@dimen/dim_7"
        android:bottomLeftRadius="@dimen/dim_7"
        android:bottomRightRadius="@dimen/dim_7" />
    <!-- Background -->
    <solid android:color="@android:color/transparent" />

    <!-- Padding -->
    <padding
        android:top="@dimen/dim_10"
        android:bottom="@dimen/dim_10"
        android:left="@dimen/dim_10"
        android:right="@dimen/dim_10" />
</shape>